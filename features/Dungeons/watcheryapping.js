import settings from '../../config'

//cancels all non imporant watcher messages

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: That one was weak anyway.")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: Things feel a little more roomy now, eh?")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: I've knocked down those pillars to go for a more...open concept.")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: Plus I needed to give my new friends some space to roam...");

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: This guy looks like a fighter.")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: I'm impressed.")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: Hmmm... this one!")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: Ouch.. just kidding.")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: Go, fight!")
register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: Very nice.")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: Aw, I liked that one.")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: Go and live again!")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: You'll do.")

register('chat', (event) => {
    if (!settings().watcheryaphider) return
    cancel(event)
}).setCriteria("[BOSS] The Watcher: Not bad.")
